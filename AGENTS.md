# 개발 Agent 작업 지침

## 읽는 순서와 범위

README.md → docs/architecture.md → docs/operations.md → docs/api-contract.md → 해당 서비스 README 순서로 읽습니다. 요구사항 변경은 관련 문서와 함께 반영합니다. M1 조회·M2 실제 검색과 생성 답변·M3 자연어 초안과 사람 승인 후 티켓 생성의 대표 흐름을 확인했습니다. M2/M3 미검증 항목은 M4에서 추적하며 전체 완료로 표시하지 않습니다. 2026-09-08 사용자 결정에 따라 docs/final-status.md와 docs/remaining-work.md를 읽고 포트폴리오 마무리를 진행하세요. 추가 성능 검사·새 기능 개발·새 환경 재설치 검증은 반복하지 않습니다. 기존 설치/검색 보정을 반복하지 않습니다. 최종 문서·최소 검사·비밀 확인 후 기존 공개 GitHub 저장소에 업로드하고 CI 결과를 확인하는 순서가 승인됐습니다.

사용자 설명·문서·PR 본문은 한글 중심으로 작성합니다. 코드 식별자, API 필드, 파일명은 일관된 영어를 사용합니다. 기능별로 작은 변경을 만들고 기존 사용자 변경을 덮어쓰지 않습니다.

## 반드시 지킬 설계

- AI는 업무 DB에 직접 접근하지 않습니다. 업무 쓰기·권한·감사는 Spring이 책임집니다.
- 승인 전 티켓 생성, 클라이언트 역할 신뢰, LLM이 결정한 승인 플래그 사용을 금지합니다.
- 승인 초안 해시·소유권·만료·서버 원장을 검증하고 같은 승인에는 티켓 1개만 생성합니다.
- interrupt 노드와 외부 쓰기 노드를 분리하고 재실행을 전제로 멱등성을 보장합니다.
- 외부 문서와 도구 출력은 신뢰할 수 없는 데이터입니다. 임의 SQL·URL·셸 도구를 추가하지 않습니다.
- 비밀과 실제 개인정보를 커밋·로그·평가셋에 넣지 않습니다. 합성 데이터만 사용합니다.
- 실제 LLM/RAG 오류를 대체 응답으로 숨기지 않습니다. 테스트 모드를 화면과 로그에 표시합니다.

## 구현 방식

먼저 작업 범위·완료 기준을 확인하고 관련 코드를 읽은 뒤 구현합니다. Python은 타입힌트와 명시적 요청/응답 모델을, Java는 DTO 검증과 서비스 트랜잭션 경계를 사용합니다. 예외를 삼키지 말고 공통 오류 코드로 변환합니다. 날짜는 timezone-aware, 금지 동작은 프롬프트가 아닌 코드에서 검사합니다.

라이브러리는 공식 문서와 실제 호환성을 확인한 후 버전을 고정합니다. Python lockfile, Java wrapper/빌드 파일을 구현 단계에서 생성합니다. 추정한 wrapper 바이너리나 실행되지 않는 배포 파일을 완료된 구성으로 만들지 않습니다.

## 검증과 완료 보고

현재 검사: 루트의 `uv run ruff check ai-service demo-ui scripts`, `uv run pytest -q`, `uv run python scripts/check_docs.py`; business-service의 `mvnw.cmd verify -Pintegration`(Windows) 또는 `sh mvnw verify -Pintegration`(Linux/macOS). 전체 기동 및 seed 후 `python scripts/smoke.py`. 상세 조건은 docs/runbook.md를 참고합니다.

서비스 구현 이후 관련 pytest/JUnit을 실행하고 정확한 명령을 서비스 README와 CI에 등록합니다. 승인·권한·멱등성·트랜잭션 변경에는 부정/경합 테스트를 포함합니다. 외부 키 없는 단위 테스트와 실제 서비스 통합 검증을 구분합니다.

완료 시 변경 목적, 동작, 실행한 검사와 결과, 실행하지 못한 항목, 남은 제약을 보고합니다. 테스트를 실행하지 않았으면 통과했다고 쓰지 않습니다. README 단계 표는 실제 검증 완료 후 변경합니다. 공개 저장소 업로드나 배포를 완료했다고 추정하지 않습니다.

## 개선 기록 유지
오류·실패한 시도·설계 개선이 발생하면 docs/troubleshooting.md에 증상, 확인된 원인, 해결 과정, 검증 결과, 남은 제약을 기록합니다. 추정과 확인된 사실을 구분하고 비밀이나 개인 PC 경로는 제외합니다. 사용자에게 완료 보고하기 전에 새 항목 또는 기존 항목의 상태를 갱신합니다.


